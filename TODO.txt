1. Лист оцінки (робота з таблицею MonitoringDictionary), це для розминки

ендпоінти:
1.1 GET для отримання списку за типом (ListType)
1.2 PATCH для редагування пунктів зі списку
1.3 POST для додавання нового пункту

багато інфи, все одразу розуміти не обов'язково)
почни з перегляду файлу з таблицями + можеш створити в себе таблицю з листу MonitoringDictionary

якщо треба зум або якісь питання - кажи, я весь день вільний
2. Юзери (як воно зараз виглядає в інтерфейсі - на скрінах)

що має бути:
0. Я забув додати ще одну таблицю UserManagers, де записується зв'язок юзера з його менеджером, можеш її пусту створити, там мають бути поля Id, UserId та ManagerId (UserId та ManagerId, як і в таблиці Monitoring - це ключі до таблиці Users)

1. Створення користувача:
1.1. тут треба для списку зверху (там де поле "Логін користувача") зробити GET ендпоінт, який буде віддавати список всіх юзерів з таблиці CollectUser (це в нас типу загальна база користувачів існуючих, а таблиця Users - це юзери саме з доступом до цього додатку), потрібно повертати лише поля id, Login та FullName
1.2. GET ендпоінт для отримання списку ролей (таблиця Roles, потрібні поля id та Name)
1.3. GET ендпоінт для отримання списку менеджерів (те, що на скріні "Безпосередній керівник"). Це обирається з таблиці Users юзери, в яких немає дати звільнення, і роль має Level < 2)
1.4. POST ендпоінт для створення нового користувача (в реквесті тобі буде надходити схема аналогічна до моделі таблиці Users + поле ManagerId, яке треба додавати до таблиці UserManagers). Коли дійдеш до цього - ще окремо поговоримо/або я трохи напишу про хешування паролю, і додамо авторизацію тут також

2 частина буде використовувати авторизацію, тому її потім окремо пропишу, що треба буде
2. Юзери (2 частина)

2.1. GET ендпоінт для отримання підлеглих (підлеглих по таблиці UsersManagers можна дивитися)
(тут треба повертати логін, ім'я, роль, дату прийому на роботу, дату звільнення, e-mail та стан випробувального терміну (Probation))
2.2. GET ендпоінт для отримання списку всіх користувачів, в яких level ролі > за level ролі користувача, що відправляє запит (тут треба повертати логін, ім'я, роль, дату прийому на роботу та дату звільнення)
2.3. PATCH ендпоінт для зміни статусу випробувального терміну (поле Probation), може змінювати тільки менеджер юзера
2.4 PATCH ендпоінт для зміни керівника юзера (може використовувати або менеджер, в якого level ролі < level-a ролі поточного менеджера, або сам поточний менеджер)
2.5. GET ендпоінт для звільнення обраного юзера (в поле DismissalDate вноситься сьогоднішня дата), може використовувати менеджер користувача
2.6. PATCH ендпоінт для зміни ролі користувача (може використовувати менеджер користувача, або менеджер, в якого level ролі < level ролі поточного менеджера), level нової ролі користувача не може бути вище level-у ролі менеджера, який надіслав запит
2.7. DELETE ендпоінт для того, щоб прибрати користувача з підлеглих.

p.s. менеджером вважається користувач з левелом ролі 0 або 1;
поточний менеджер користувача - це менеджер, в якого в таблиці UsersManagers прописаний зв'язок з іншим користувачем

3 частина (Моніторинги) 🙃

0. Я додав у файл ще дві таблички (CollectContract і CollectReestr, вони пов'язані через поле ReestrId в CollectContract до id з CollectReestr), вони знадобляться тут, треба для них теж створити модельки і завантажити

0/1. Ще я оновив BaseDictionary, щоб трохи спростити тобі, бо в оригінальній базі там трешак відбувається, який я так і не зміг пофіксити, бо мені за 3 роки роботи так прав і не дали 😅

1. GET ендпоінт для отримання інформації за айди контракту (id з CollectContract), треба повернути суму боргу (SummToClose), номер реєстру (тут це не айдішка, а поле RNumber з CollectReestr) і назву реєстру (Name з CollectReestr)

2. GET ендпоінт, який повертає певні дані зі словника (BasicDictionary, повертати треба id та Name). Можливі категорії:
- "контакт з": треба повертати рядки, в яких SpecId=2
- "Результат дзвінка" (тут додатково буде надаватися обраний варіант з категорії "контакт з"): якщо "Должник" або "Поручитель" - треба повертати рядки, в яких SpecId=(3 або 4); для "Родственник" або "Третье лицо" - SpecId=(4 або 5)
- "Тип дзвінка": рядки, в яких SpecId=6
- "Дисконт/реструктуризація": рядки, в яких SpecId=1

3. POST ендпоінт для створення запису в таблиці Monitoring (на вході будуть всі поля, окрім MonitoringDate (це буде сьогоднішня дата/час в момент створення запису) та ManagerId(його діставати з авторизації). Повертати треба айдішку нового запису в таблиці Monitoring.

4. POST ендпоінт для створення записів в таблиці MonitoringScores. Тут на вхід подається об'єкт з айдішкою моніторингу (ту саму, що отримали у пункті 3) і списком айдішек з таблиці MonitoringDictionary. Треба за цим списком створити записи в таблиці MonitoringScores.

5. GET ендпоінт для отримання інформації по моніторингам. Треба повернути дані як на третьому скріні (тут контрагент - це Name з CollectReestr, НКС - це id з CollectContract (він же ContractId в Monitoring); реєстр - це RNumber з CollectReestr: інші, якщо не зрозуміло за назвою - то питай).
Щодо загального балу - тут такий алгоритм:
- є id з Monitoring;
- за цим id можна отримати список ScoreTypeId з MonitoringScores
- ScoreTypeId - це id з MonitoringDictionary, за яким можна отримати список коефіцієнтів (Coefficient)
- сума такого отриманого списку коефіцієнтів і є загальним балом

6. Додати до ендпоінту з 5 пункту фільтри (за датами, за айдішкою оператора (юзера), за "ПІБ оцінщика" та за "ПІБ супервізора", можеш ще якісь придумати 🙃)

p.s. "ПІБ оцінщика" - це ім'я за тою айдішкою, яку ти будеш витягувати з авторизації при створенні моніторингу
"ПІБ супервізора" - це ім'я за айдішкою менеджера (того, що в UsersManagers прив'язаний)